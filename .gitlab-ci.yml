image: python:3.9-alpine

pages:
  stage: deploy
  script:
  - pip install -U sphinx
  - cd docs
  - make html
  - cd ..
  - mv docs/build/html public/
  artifacts:
    paths:
    - public
  only:
  - main
