name: "Rust CI"

on:
  push:
    paths:
      - ".github/workflows/rust.yaml"
      - '**.rs'
      - '**/Cargo.toml'
      - '**/Cargo.lock'

jobs:
  lint:
    uses: darbiadev/.github/.github/workflows/rust-lint.yaml@e3ebedcaeee8d40bdb7ef569dacd74829ab0c368 # v14.0.0

  clippy:
    needs: lint
    uses: darbiadev/.github/.github/workflows/rust-clippy.yaml@e3ebedcaeee8d40bdb7ef569dacd74829ab0c368 # v14.0.0

  test:
    needs: "clippy"
    strategy:
      matrix:
        triple:
          - {
            os: "ubuntu-latest",
            target: "x86_64-unknown-linux-gnu",
          }

    uses: darbiadev/.github/.github/workflows/rust-test.yaml@e3ebedcaeee8d40bdb7ef569dacd74829ab0c368 # v14.0.0
    with:
      os: ${{ matrix.triple.os }}
      target: ${{ matrix.triple.target }}
